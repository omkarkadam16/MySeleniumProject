names = ["Omkar", "Ashi", "Neha"]
for index, name in enumerate(names):
    print(index, name)

"enumerate(names) generates pairs of index and name from the list."
"The loop assigns:"
"index → The position of the item (starting from 0)."
"name → The value at that position."
